---
work_package_id: WP02
title: Execute examples 01, 03, 04, 05
dependencies: []
requirement_refs:
- FR-003
- FR-005
subtasks:
- T006
- T007
- T008
- T009
- T010
- T011
phase: Phase 2 - Execution
history:
- at: '2026-09-23T05:05:00Z'
  actor: system
  action: Prompt generated via /spec-kitty.tasks
agent_profile: implementer-ivan
authoritative_surface: examples/
create_intent:
- kitty-specs/verify-examples-01M36AAT/results/wp02-results.json
execution_mode: code_change
model: ''
owned_files:
- examples/01-monorepo-build/**
- examples/03-multi-language-build/**
- examples/04-incremental-testing/**
- examples/05-ml-workflows/**
- kitty-specs/verify-examples-01M36AAT/results/wp02-results.json
role: implementer
tags: []
task_type: implement
tracker_refs: []
---

# Work Package Prompt: WP02 – Execute examples 01, 03, 04, 05

## ⚡ Do This First: Load Agent Profile

Use the `/ad-hoc-profile-load` skill to load the agent profile specified in the frontmatter, and behave according to its guidance before parsing the rest of this prompt.

- **Profile**: `implementer-ivan`
- **Role**: `implementer`
- **Agent/tool**: `opencode`

## Objectives & Success Criteria

- Execute each owned example's `test.sh` (or documented primary target) under a timeout and record the result.
- Audit every documented `gaffer-exec` command in the owned examples against `gaffer-exec 0.8.0`.
- Fix any non-environment defect found, inside the owning example only.

## Context & Constraints

- Spec: [spec.md](../spec.md) (FR-003, FR-005; NFR-002, NFR-003, NFR-004).
- Plan: [plan.md](../plan.md) (IC-02, IC-03).
- Harness from WP01: `kitty-specs/verify-examples-01M36AAT/verify.sh`.
- Result vocabulary: `pass`, `fail`, `blocked (environment)`. Never label a missing-toolchain case `fail`.
- Never print `.env` values. Bound every execution (<= 10 min).

## Branch Strategy

- **Strategy**: Planning artifacts were generated on `verify-examples`; completed changes must merge back into `verify-examples`.
- **Planning base branch**: `verify-examples`
- **Merge target branch**: `verify-examples`

## Subtasks & Detailed Guidance

### Subtask T006 – Example 01 (monorepo-build)

- **Purpose**: Prove the TypeScript monorepo builds, caches, and runs.
- **Steps**: `cd examples/01-monorepo-build && bash test.sh`; capture exit code + summary. Requires `node`/`npm`; else `blocked (environment: node)`.
- **Files**: `examples/01-monorepo-build/test.sh` (fix only if defective)

### Subtask T007 – Example 03 (multi-language-build)

- **Purpose**: Prove Rust/Go/Node/Python builds.
- **Steps**: Run `bash test.sh` with a timeout. Record which toolchains are present; `blocked (environment)` if a required one is missing.

### Subtask T008 – Example 04 (incremental-testing)

- **Purpose**: Prove test orchestration + caching.
- **Steps**: Run `bash test.sh`. Allow bounded retries for the flaky-test demonstration; report variance.

### Subtask T009 – Example 05 (ml-workflows)

- **Purpose**: Prove the Python ML pipeline runs.
- **Steps**: Run `bash test.sh`. The `setup` target creates a venv (~1 GB); if creation fails, record `blocked (environment: python3/pip)`.

### Subtask T010 – Documentation command audit (owned examples)

- **Purpose**: Ensure docs match the CLI.
- **Steps**: Extract `gaffer-exec ...` lines from `*.md`/`*.sh`/`package.json` in the owned examples; confirm every flag/subcommand exists in `gaffer-exec --help`. Record violations.

### Subtask T011 – Fix defects in place

- **Purpose**: Leave examples green.
- **Steps**: For each non-environment `fail`, fix within the owning example; re-run to confirm. If unfixable without out-of-scope changes, record the defect + rationale for WP05.

## Test Strategy

- Re-run each fixed example's `test.sh` and confirm exit 0.

## Risks & Mitigations

- Long installs → run with `timeout`; treat timeouts caused by installs as `blocked (environment)` with a note.
- `05` venv is large → do not commit it; it is gitignored.

## Review Guidance

- Confirm each result cites the exact command and raw outcome; confirm fixes stayed within owned files.

## Activity Log

- 2026-09-23T05:05:00Z – system – Prompt generated via /spec-kitty.tasks

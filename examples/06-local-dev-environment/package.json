{
  "name": "local-dev-environment",
  "version": "1.0.0",
  "description": "Local Development Environment Example for gaffer-exec",
  "private": true,
  "scripts": {
    "setup": "gaffer-exec --workspace-root . run make:setup",
    "dev": "gaffer-exec --workspace-root . run make:dev",
    "test": "gaffer-exec --workspace-root . run make:test",
    "stop": "gaffer-exec --workspace-root . run make:stop",
    "clean": "gaffer-exec --workspace-root . run make:clean"
  },
  "keywords": ["gaffer-exec", "local-development", "postgres", "express", "react"],
  "author": "Local Dev Environment Example",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
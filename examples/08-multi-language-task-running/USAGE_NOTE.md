# Note: package.json auto-discovery interferes with manual graph.json

To use this example, either:
1. Rename package.json files temporarily, OR
2. Use explicit graph override: gaffer-exec --graph-override graph.json

This is a known limitation of gaffer-exec's auto-discovery feature.

